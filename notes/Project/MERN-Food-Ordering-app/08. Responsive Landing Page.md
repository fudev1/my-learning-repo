
**Créer un composant Hero.tsx**

```tsx
import hero from "../assets/hero.png";

const Hero = () => {
  return (
    <div>
	    // image qui prend une hauteur max de 600px et qui ne s'étire pas mais s'ajuste au content
        <img src={hero} alt="hero img" className="w-full max-h-[600px] object-cover" />
    </div>
  )
}

export default Hero;
```

**Editer le fichier Layout.tsx pour ajouter le Hero**

```tsx
import Header from "@/components/Header";
import Hero from "@/components/Hero";

type Props = {
    children: React.ReactNode;
}

const Layout = ({ children }: Props) => {
    return (
        <div className="flex flex-col min-h-screen">
            <Header />
            // Ajouter le Hero
            <Hero />

            <div className="container mx-auto flex-1 py-10">
                {children}
            </div>
        </div>
    )
}

export default Layout;
```