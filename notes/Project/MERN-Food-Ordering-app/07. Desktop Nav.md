**Créer un composant MainNav.tsx** 

```tsx
import { Button } from "./ui/button";

const MainNav = () => {
  return (
    <Button variant="ghost" className="font-bold hover:text-orange-500 hover:bg-white">Se connecter</Button>
  )
}

export default MainNav;
```

**Editer le fichier Header.tsx pour ajouter le composant**
```tsx
import { Link } from "react-router-dom";
import MobileNav from "./MobileNav";
import MainNav from "./MainNav";


const Header = () => {
  return(
    <div className="border-b-2 border-b-orange-500 py-6">
        <div className="container mx-auto flex justify-between items-center">

            <Link 
                to='/' 
                className="text-3xl font-bold tracking-tight text-orange-500">
                MERNEATS.com
            </Link>

            <div className="md:hidden"> 
                <MobileNav />
            </div>

			// AJOUTER CETTE LIGNE DANS LE HEADER
            <div className="hidden md:block">
              <MainNav />
            </div>

        </div>
    </div>
  )
}

export default Header;
```